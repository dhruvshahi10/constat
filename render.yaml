services:
  - type: web
    name: pramana
    runtime: docker
    plan: starter
    numInstances: 1  # global run queue + SQLite are single-process by design
    healthCheckPath: /healthz
    disk:
      name: pramana-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PRAMANA_DATA
        value: /data
