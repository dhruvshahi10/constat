services:
  - type: web
    name: constat
    runtime: docker
    plan: starter
    numInstances: 1  # global run queue + SQLite are single-process by design
    healthCheckPath: /healthz
    disk:
      name: constat-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: CONSTAT_DATA
        value: /data
