# Runtime. Exact pins: the Dockerfile installs this same file, so the image
# and a local venv resolve to the identical set. Bump deliberately, together.
openpyxl==3.1.5        # questionnaire read/write (the only certified ingest path)
pypdf==6.16.1          # PDF upload extraction
python-docx==1.2.0     # DOCX upload extraction
fastembed==0.8.0       # semantic retrieval; falls back to ngram if absent

# Optional extras, deliberately NOT installed in the image:
#   anthropic>=0.40    # AnthropicDrafter only; the hosted drafter is Gemini,
#                      # which speaks REST over stdlib urllib and needs no SDK.
#   pytest==9.1.1      # dev/CI only — see requirements-dev.txt
